/* ============================================================
   Где хранятся данные (заказы, меню, настройки, промокоды, фото).
   По умолчанию — папка data/ внутри проекта.
   В облаке задайте DATA_DIR на путь постоянного диска (например, /data на Amvera),
   тогда данные переживут пересборку приложения.
   ============================================================ */
'use strict';

const path = require('path');

const DATA_DIR = process.env.DATA_DIR
  ? path.resolve(process.env.DATA_DIR)
  : path.join(__dirname, '..', 'data');

module.exports = { DATA_DIR };
